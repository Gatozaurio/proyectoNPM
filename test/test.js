describe('#suma', function() {
    it('debe devolver la suma de los dos valores introducidos', function() {
      expect(suma(1,1)).to.equal(2);
    });
});
describe('#suma', function() {
    it('debe devolver la suma de los dos valores introducidos', function() {
      expect(suma(1,2)).not.equal(2);
    });
});